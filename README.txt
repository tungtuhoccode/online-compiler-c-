
Command: 
1. sudo docker build -t gcompiler .
-  Build a docker images using the dockerfile, with the name "gcompiler"

2. docker run -it gcompiler /bin/bash
- Run docker image and get the output in the host terminal

docker mount vs volume